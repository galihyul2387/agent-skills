# القواعد العامة للمساعد الذكي والمطورين — MyAgent (متعدد اللغات / Polyglot)

[ 🇮🇩 Bahasa Indonesia ](AGENTS.md) | [ 🇬🇧 English ](AGENTS.en.md) | [ 🇨🇳 简体中文 ](AGENTS.zh.md) | [ 🇸🇦 العربية ](AGENTS.ar.md)

---

## اختيار اللغات وواجهة المستخدم متعددة اللغات (i18n & RTL)
*   **اختيار لغة الوثائق:** يمكن إعداد الوثائق الفنية، ووثائق المتطلبات (PRD)، وقصص المستخدم، وخطط الاختبار، وحالات الاختبار، وتصاميم البنية التحتية، وتقارير التحليل إما بـ **اللغة الإندونيسية (الافتراضية)** أو **اللغة الإنجليزية** وفقاً لتفضيل المستخدم أو متطلبات الفريق.
*   **دعم 4 لغات لواجهة المستخدم (i18n & RTL):** تدعم واجهة المستخدم التبديل اللحظي بين 4 لغات رئيسية: **🇮🇩 الإندونيسية (ID)**، **🇬🇧 الإنجليزية (EN)**، **🇨🇳 الصينية (ZH - المبسطة)**، و **🇸🇦 العربية (AR - مع الدعم الكامل لتخطيط RTL من اليمين إلى اليسار)**.
*   **اتساق الوثيقة الواحدة:** يجب الالتزام بلغة واحدة متسقة داخل الوثيقة نفسها (عدم خلط اللغات داخل نفس الملف).
*   **التسميات التقنية القياسية:** جميع أسماء المتغيرات، والدوال، والطرق (Methods)، والفئات (Classes)، والواجهات (Interfaces)، وأسماء الملفات، ومسارات API، ورسائل الالتزام (Commit messages) **يجب أن تبقى 100% باللغة الإنجليزية القياسية للبرمجة**.

## المعايير البرمجية العامة المستقلة عن اللغة (Language-Agnostic)
*   **الكود النظيف والمسؤولية الواحدة (SRP):** يجب أن تقتصر كل دالة/طريقة على أداء مسؤولية واحدة فقط.
*   **الحد الأقصى لطول الدالة:** الحد الأقصى لطول الدالة الواحدة هو **50 سطراً** — ويجب تقسيمها إلى دوال فرعية أو مساعدة (Helpers) إذا تجاوزت ذلك.
*   **التحقق الصارم من الأنواع (Strong Typing):** تفعيل التحقق الصارم من الأنواع في جميع اللغات المدعومة (Strict mode في TypeScript، و Type Hints في Python، و `declare(strict_types=1)` في PHP، و Nullable Reference Types في C#، والأنواع الثابتة في Java/Go/Rust).
*   **أولوية القيم غير القابلة للتعديل (Immutability First):** اعتماد القيم غير القابلة للتعديل افتراضياً (`const` في JS/TS، و `readonly`/`record` في C#، و `val` في Kotlin).
*   **معالجة الأخطاء الصريحة:** معالجة الاستثناءات والأخطاء بشكل صريح — يمنع ترك كتل catch فارغة أو تجاهل قيم الأخطاء المرجعة.

## المعايير النموذجية لكل لغة برمجة

| لغة البرمجة | معيار التنسيق | أدوات التحليل الساكن (Linter) | حزم الاختبار | المبادئ الأساسية |
|---|---|---|---|---|
| **TypeScript / Node.js** | Prettier | ESLint (`@typescript-eslint`) | Jest / Vitest / Mocha | Strict mode, ES6+, يمنع استخدام `var` أو `any` بدون مبرر قوي |
| **Python** | Black / Ruff | Flake8 / Ruff / MyPy | PyTest / Unittest | PEP 8, Type Hints, Virtualenv, Poetry/Pipenv |
| **Golang** | `gofmt` / `goimports` | `golangci-lint` | `go test` | الأسلوب النموذجي لـ Go, فحص الأخطاء الصريح, انعدام الـ panic |
| **Java / Kotlin** | Spotless / Google Java | Checkstyle / SonarLint / Detekt | JUnit 5 / Mockito | SOLID, البنية النظيفة, Lombok/Records, Spring/Micronaut |
| **PHP** | PHP-CS-Fixer (PSR-12) | PHPStan (Level 8+) / Psalm | PHPUnit / Pest | `declare(strict_types=1);`, الخصائص المعرفة بالنوع, Composer |
| **C# / .NET 10** | `dotnet format` | Roslyn Analyzers / StyleCop | xUnit / NUnit | .NET 10 LTS, C# 13+, Nullable مفعل, البناء الأساسي, غير متزامن |
| **Rust** | `rustfmt` | Clippy | `cargo test` | Rust النموذجية, يمنع استخدام `unsafe` بدون مبرر صارم |

## الأمان العام (DevSecOps)
*   **انعدام الأسرار المشفرة ثابتاً (Zero Hardcoded Secrets):** يُحظر تماماً تضمين مفاتيح API أو كلمات المرور أو الرموز السرية داخل الكود المصدري.
*   استخدام ملفات إعدادات البيئة دائماً (`.env`, `appsettings.json`, `application.yml`, `config.yaml`) مع إضافتها إلى `.gitignore`.
*   تطبيق مبدأ **أقل الصلاحيات (Least Privilege)** على جميع مستويات قواعد البيانات، وبوابات API، والحاويات.
*   تشفير كافة قنوات الاتصال بالبيانات عبر بروتوكولات **HTTPS / TLS 1.3**.

## معايير التوثيق والجودة
*   يجب أن يتضمن كل ملف إعدادات ومنطق معقد تعليقات توضيحية شارحة.
*   توثيق جميع نقاط نهاية API باستخدام معيار **Swagger / OpenAPI 3.0**.
*   يجب أن يجتاز الكود فحص **الـ Linters وأدوات التنسيق** بنجاح قبل الالتزام.
*   استهداف نسبة تغطية اختبارات لا تقل عن **80%** للمنطق التجاري الأساسي.
*   تسجيل كافة التغييرات الجوهرية في ملف **CHANGELOG.md** والمرور عبر عملية **مراجعة الكود (Pull Request Code Review)**.
